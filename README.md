# Hospital-Management-System---V2

A role-based Hospital Management System built with **Flask (API)** + **Vue.js (UI)** + **SQLite** + **Redis + Celery** for background jobs.
The system supports roles of:

- Admin
- Doctor
- Patient

It handles user authentication, doctor and patient management, appointment booking, treatment history, and automated reminder/report jobs.
